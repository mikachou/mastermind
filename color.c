#include "color.h"

/*Color * Color_create(BaseColor c)
{
    Color * color = malloc(sizeof(Color));
    
    color->color = c;
    
    Color_setRGB(color)
    
    return color;
}

void Color_initRGB(Color * color)
{
    int base[] = baseColorRGB[color->color];
    
    color->r = base[0];
    color->g = base[1];
    color->b = base[2];
}

void Color_destroy(Color * color)
{
    free(color);
}*/